server:
  port: 0
spring:
  application:
    name: reactive-url-shortener-test
  r2dbc:
    url: r2dbc:postgresql://test/shortener
    username: username
    password: password
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost
app:
  short-link-base: http://localhost
